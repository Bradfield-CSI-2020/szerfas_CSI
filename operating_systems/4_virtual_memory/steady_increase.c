# include <stdio.h>
# include <stdlib.h>

int main() {
    int count;
    int *mem_pointer;
    while(1) {
        mem_pointer = malloc(4096 * 10);
        printf("mem allocated: %d", count += (4096* 10);
    }
}